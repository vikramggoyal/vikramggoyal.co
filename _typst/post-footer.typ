// _typst/post-footer.typ
// Loaded via include-in-header in _metadata.yml.
// Sets a default article URL; each post overrides #article-url before this is applied.
#let article-url = "vikramggoyal.co"   // default; overridden per-post in frontmatter
